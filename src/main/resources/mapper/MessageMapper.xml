<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzzlew.zzzimserver.mapper.MessageMapper">

    <insert id="saveMessage"
            parameterType="com.zzzlew.zzzimserver.pojo.dto.message.MessageDTO">
        insert into message (conversation_id, sender_id, receiver_id, content, msg_type)
        values (#{conversationId}, #{senderId}, #{receiverId}, #{content}, #{msgType})
    </insert>

    <select id="getMessageList" resultType="com.zzzlew.zzzimserver.pojo.vo.message.MessageVO"
            parameterType="java.lang.String">
        select *
        from message
        where conversation_id = #{conversationId} order by send_time
    </select>

</mapper>